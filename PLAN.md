# Coffee Bean Tracker — 機能計画

買ったコーヒー豆を記録するだけのアプリから、「次に何を買うか」を考える助けになるツールへ。
このドキュメントは確定仕様ではなく、追加したい機能のブレストとアイデアプール。

---

## 現状のおさらい

- 端末ローカル（IndexedDB / Dexie）に購入記録を保存
- 記録項目: 日付, 国, 地域, 豆名, 焙煎所, グラム, プロセス, テイスティングノート
- 検索 / 国・プロセスでのフィルタ / ソート
- 統計: 件数, 総グラム, 国・焙煎所のユニーク数, 上位国
- JSON エクスポート / インポート

---

## 全体方針（決定事項）

- **スマホファースト**。レイアウト・タップ領域・フォントサイズはモバイル基準で最適化し、PC は副次。
- **評価軸は 5 つ星**（半星なし）。100 点や SCA カッピングフォームは採用しない。
- **抽出（淹れ方）ログは作らない**。別アプリの領域として切り出す。本アプリは「買った豆の記録」に集中する。

---

## 優先して取り組むもの（短期）

ここから着手する。実装コストが比較的低く、ユーザー体験への効果が大きい順。

### 1. 記録項目の拡張

今のままだと振り返りに情報が足りない。`Purchase` 型に optional フィールドを追加し、
既存レコードは undefined のままで動くようにする（マイグレーション不要）。

追加候補:

- **焙煎度**（ライト / ミディアム / ミディアムダーク / ダーク。enum）
- **焙煎日**（roastDate。鮮度の目安）
- **価格**（円。100g 単価の自動計算に使う）
- **品種**（variety: Geisha, SL28, Bourbon, Typica, … 自由入力 + サジェスト）
- **農園 / 生産者**
- **標高**（altitude, m）
- **自己評価**（★1〜5）

フォームは段階的に拡張する。詰め込みすぎるとスマホで縦に長くなりすぎるので、
「基本」と「詳細（折りたたみ）」に分けることを検討。

### 2. 焙煎所オートコンプリート

過去に入力した `roaster` から候補をサジェスト。
表記揺れ（半角/全角、空白、株式会社の有無）を抑えるだけで、後の統計が劇的に綺麗になる。

将来的には豆名や品種、農園にも横展開する。

### 3. PWA 対応

`vite-plugin-pwa` を導入して、ホーム画面追加 / オフライン起動を可能にする。
スマホでメモする用途と相性が良い。アイコンとスプラッシュも用意。

### 4. ダークモード

Tailwind の `dark:` クラスを利用。トグルと「OS 設定に従う」をローカルストレージに保存。
スマホで夜にメモするときの眩しさ対策。

---

## 中期: データが貯まってから効いてくる機能

### 5. テイスティングノートの構造化（採用）

自由記述に加えて構造化された評価を入れる。

- 酸味 / 甘味 / 苦味 / ボディ / 後味 を 5 段階で
- フレーバータグ（ベリー、シトラス、チョコ、ナッツ、フローラル … SCA Flavor Wheel ベース）
- レーダーチャートで可視化（豆カード詳細画面）

スマホ前提なので、5 軸のスライダー入力 + タップでタグ追加の UI に。

### 6. 統計の強化（将来）

- 月別 / 年別の購入量・支出グラフ（recharts など）
- **世界地図ビュー**: **正射方位図法（Orthographic Projection）** を採用し、
  地球の一部分だけを大きな球面として表示する。
  - スマホ縦画面いっぱいに表示し、ドラッグで回転、ピンチでズーム
  - 購入実績のある国にピン / 色付け（件数で濃淡）
  - タップでその国の購入履歴へドリルダウン
  - 「まだ買っていない国」も控えめに可視化して、次の購入のヒントに
  - 実装は d3-geo の `geoOrthographic` あたりが第一候補
- 焙煎所のリピート率、プロセス別の平均評価などのインサイト
- 「まだ買っていない国 / プロセス」のサジェスト

### 7. 検索 / フィルタ強化（将来的に拡張）

- テイスティングノートも全文検索対象に
- 複合フィルタ（国 × プロセス × 期間 × 評価）
- 「お気に入り」フラグとそのフィルタ
- フレーバータグでの絞り込み（5 で追加するタグと連動）

### 8. 在庫管理

- 「残量(g)」フィールドを手動更新で運用（自動減算はしない。抽出ログを作らないため）
- ホーム画面に「今手元にある豆」セクション

---

## クラウド同期 / バックアップ

ローカル DB のみだと端末をまたげない。やるなら段階的に。

### 9. 自動バックアップ

- 変更のたびに JSON を localStorage にスナップショット（直近 N 件）
- 「最後のバックアップから XX 日経過」警告

### 10. クラウド同期（要検討）

選択肢:

- **GitHub Gist** に PAT で push（自分専用、無料）
- **Google Drive / Dropbox**（OAuth）
- **Supabase / Firebase**（多端末リアルタイム同期、要バックエンド）

MVP は Gist が一番安い。トークンは IndexedDB に保存。

---

## UI / UX

### 11. 表示モード切り替え

カード（既定、モバイル向け）/ リスト（密度高め）/ テーブル（PC 向け）の 3 モード。

### 12. 一覧から直接編集

少なくとも「残量」「評価」だけでもタップで即編集できると便利。

### 13. キーボードショートカット（PC 向け）

`N` で新規追加、`/` で検索フォーカス、`Esc` で閉じる。
スマホファースト方針なので優先度は低い。

### 14. 国際化 (i18n)

日本語 / 英語切り替え。`react-i18next` あたり。

---

## 共有 / 公開

### 15. 個別の豆を URL でシェア

`#/purchase/<id>` のような hash route。データは URL に base64 で埋め込む or Gist 経由。

### 16. 公開モード

自分の購入履歴を read-only で公開する URL を発行。コーヒー日記ブログ的に使える。

---

## 入力支援

### 17. 焙煎所サイトからのスクレイピング

URL を貼ると、商品名 / 国 / プロセスなどを自動で埋める。
CORS の壁があるので、Cloudflare Worker などのプロキシ前提。

### 18. 写真添付 + OCR

豆のパッケージ写真を添付。Tesseract.js でテキスト抽出してフォームを埋める。

### 19. CSV インポート / エクスポート

スプレッドシート派の人向け。

---

## 技術的な改善（地味だけど効く）

- **テスト**: Vitest で `db/purchases.ts` と `utils/io.ts` をユニットテスト
- **E2E**: Playwright で「追加 → 編集 → 削除 → エクスポート → インポート」の通しシナリオ
- **エラー境界**: ErrorBoundary を追加して、IndexedDB 破損時に画面が真っ白にならないように
- **マイグレーション戦略**: Dexie のバージョニングを使った schema 変更の練習
- **アクセシビリティ**: フォームの label, aria 属性, タップ領域 44px 以上の徹底
- **バンドルサイズ**: 国データ（countries.ts）の遅延ロード
- **モバイル動作確認**: 実機 Safari/Chrome での縦・横画面と Safe Area 対応

---

## 議論したいこと（決めかね）

- クラウド同期は本当に要るか（複数端末で使うかどうか次第）
- フレーバータグの語彙を SCA 準拠にするか、自由タグ + 候補サジェストにするか
- 公開モードのプライバシー設定（リンクを知っている人だけ vs 完全公開）

---

## 採用しないことに決めたもの

- **抽出（淹れ方）ログ**: 本アプリの責務外。買った豆の記録に集中する。
  抽出記録が欲しくなったら別アプリとして切り出す。
- **100 点 / SCA カッピングフォーム形式の評価**: 5 つ星に一本化。
- **PC ファースト UI**: スマホファーストに統一。PC レイアウトはあくまで副次。
