# Coffee Bean Tracker — 機能計画

買ったコーヒー豆を記録するだけのアプリから、「次に何を買う / どう淹れるか」を考える助けになるツールへ。
このドキュメントは確定仕様ではなく、追加したい機能のブレストとアイデアプール。

---

## 現状のおさらい

- 端末ローカル（IndexedDB / Dexie）に購入記録を保存
- 記録項目: 日付, 国, 地域, 豆名, 焙煎所, グラム, プロセス, テイスティングノート
- 検索 / 国・プロセスでのフィルタ / ソート
- 統計: 件数, 総グラム, 国・焙煎所のユニーク数, 上位国
- JSON エクスポート / インポート

---

## 次に着手したい候補（短期 / 1〜2 機能ずつ）

優先度高め。実装コストも低めで効果が出やすそうなもの。

### 1. 記録項目の拡張

「あとから振り返るときに欲しかった情報」が今は足りない。

- **焙煎度**（ライト / ミディアム / ダーク など。enum）
- **焙煎日**（roastDate。鮮度の目安に使える）
- **価格**（円。100g 単価の自動計算に使う）
- **品種**（variety: Geisha, SL28, Bourbon, Typica, …。自由入力 + サジェスト）
- **農園 / 生産者**
- **標高**（altitude, m）
- **自己評価**（★1〜5 もしくは 100 点）

→ `Purchase` 型に optional フィールドを追加し、フォームを段階的に拡張。
   既存レコードは undefined のままで OK（マイグレーション不要）。

### 2. 焙煎所オートコンプリート

過去に入力した `roaster` から候補をサジェスト。
表記揺れ（半角/全角、空白）を減らすだけで、後の統計が一気に綺麗になる。

### 3. ダークモード

Tailwind で `dark:` クラス追加 + ローカルストレージにトグル保存。

### 4. PWA 対応

`vite-plugin-pwa` を入れて、ホーム画面追加 / オフライン起動。
スマホでメモする用途と相性が良い。

---

## 中期: 「使い込めるアプリ」にする機能

データが貯まってから効いてくるもの。

### 5. 抽出（淹れ方）ログ

豆ごとに、淹れた記録を紐付けて残す。

- 抽出方法（V60, Kalita, French Press, Aeropress, Espresso, …）
- 豆量(g) / 湯量(g) / 湯温(℃) / 抽出時間 / 挽き目
- 出来栄えメモ + 5段階評価
- 「同じ豆を次淹れるときに参照したいレシピ」を保存

→ `Brew` テーブルを Dexie に追加し、`purchaseId` で 1:N。

### 6. テイスティングノートの構造化

自由記述に加えて:

- 酸味 / 甘味 / 苦味 / ボディ / 後味 を 5 段階で
- フレーバータグ（ベリー、シトラス、チョコ、ナッツ、フローラル … SCA Flavor Wheel 準拠）
- レーダーチャートで可視化

### 7. 統計の強化

- 月別 / 年別の購入量・支出グラフ（recharts など）
- 国マップ（世界地図に色付け）
- 「まだ買っていない国 / プロセス」サジェスト
- 焙煎所のリピート率

### 8. 在庫管理

- 「残量(g)」フィールド
- 抽出ログを記録するたびに自動で減算
- ホーム画面に「今手元にある豆」セクション

### 9. 検索 / フィルタ強化

- テイスティングノートも全文検索対象に
- 複合フィルタ（国 × プロセス × 期間 × 評価）
- 「お気に入り」フラグとそのフィルタ

---

## クラウド同期 / バックアップ

ローカル DB のみだと端末をまたげない。やるなら段階的に。

### 10. 自動バックアップ

- 変更のたびに JSON を localStorage にスナップショット（直近 N 件）
- 「最後のバックアップから XX 日経過」警告

### 11. クラウド同期（要検討）

選択肢:

- **GitHub Gist** に PAT で push（自分専用、無料、Public/Secret 選べる）
- **Google Drive / Dropbox**（OAuth、家族と共有しやすい）
- **Supabase / Firebase**（多端末リアルタイム同期、ただし要バックエンド）

→ MVP は Gist が一番安く済む。トークンは IndexedDB に保存。

---

## UI / UX

### 12. 表示モード切り替え

カード / リスト（密度高め）/ テーブル の 3 モード。テーブルは Sort/Filter と相性◎。

### 13. 一覧から直接編集

クリックでインライン編集（少なくとも「残量」「評価」だけでも）。

### 14. キーボードショートカット

`N` で新規追加、`/` で検索フォーカス、`Esc` で閉じる。

### 15. 国際化 (i18n)

日本語 / 英語切り替え。`react-i18next` あたり。

---

## 共有 / 公開

### 16. 個別の豆を URL でシェア

`#/purchase/<id>` のような hash route。
データは URL に base64 で埋め込む or Gist 経由。

### 17. 公開モード

自分の購入履歴を read-only で公開する URL を発行。
コーヒー日記ブログ的に使える。

---

## 入力支援（あると嬉しい）

### 18. 焙煎所サイトからのスクレイピング

URL を貼ると、商品名 / 国 / プロセスなどを自動で埋める。
CORS の壁があるので、Cloudflare Worker などのプロキシ前提。

### 19. 写真添付 + OCR

豆のパッケージ写真を添付。Tesseract.js でテキスト抽出してフォームを埋める。

### 20. CSV インポート / エクスポート

スプレッドシート派の人向け。

---

## 技術的な改善（地味だけど効く）

- **テスト**: Vitest で `db/purchases.ts` と `utils/io.ts` をユニットテスト
- **E2E**: Playwright で「追加 → 編集 → 削除 → エクスポート → インポート」の通しシナリオ
- **エラー境界**: ErrorBoundary を追加して、IndexedDB 破損時に画面が真っ白にならないように
- **マイグレーション戦略**: Dexie のバージョニングを使った schema 変更の練習
- **アクセシビリティ**: フォームの label, aria 属性, キーボード操作の徹底
- **バンドルサイズ**: 国データ（countries.ts）の遅延ロード

---

## 議論したいこと（決めかね）

- スマホファーストにするか PC ファーストにするか
- クラウド同期は本当に要るか（複数端末で使うかどうか次第）
- 評価軸を「100 点」にするか「5 つ星」にするか「SCA カッピングフォーム」にするか
- 抽出ログまで入れるとアプリが太る。別アプリ化も検討余地あり

---

## 採用しないことに決めたもの

（まだなし。アイデアを捨てたらここに残骸として書いていく）
